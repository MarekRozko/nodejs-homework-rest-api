const contrlWrapper = require("./contrlWrapper");
const validateBody = require("./validateBody");
const handleMongooseError = require("./handleMongooseError");

module.exports = {
    contrlWrapper,
    validateBody,
    handleMongooseError,
}